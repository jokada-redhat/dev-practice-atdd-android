<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="20dp"
    app:cardBackgroundColor="@color/surface_container_lowest"
    app:cardCornerRadius="12dp"
    app:cardElevation="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <!-- Book Title + Borrowed Date -->
        <TextView
            android:id="@+id/textLoanBookTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="18sp"
            android:textColor="@color/on_surface"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/textLoanBorrowedDate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textColor="@color/on_surface_variant"
            android:layout_marginTop="4dp"
            android:layout_marginBottom="12dp" />

        <!-- Borrower Row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="16dp">

            <TextView
                android:id="@+id/textBorrowerInitials"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:gravity="center"
                android:textSize="12sp"
                android:textColor="@color/on_primary"
                android:textStyle="bold"
                android:background="@drawable/status_badge_background"
                android:layout_marginEnd="8dp" />

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/textBorrowerName"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="14sp"
                    android:textColor="@color/on_surface"
                    android:textStyle="bold" />

                <TextView
                    android:id="@+id/textBorrowerId"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="11sp"
                    android:textColor="@color/on_surface_variant" />

            </LinearLayout>

        </LinearLayout>

        <!-- Return Button (full width) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/buttonReturnBook"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="@string/return_button_short"
            android:textSize="16sp"
            android:textColor="@color/primary"
            app:cornerRadius="24dp"
            app:icon="@drawable/ic_assignment_return"
            app:iconGravity="textStart"
            app:iconSize="20dp"
            app:iconTint="@color/primary"
            app:strokeColor="@color/outline_variant" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
